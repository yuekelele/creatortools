import React, { Component, useState } from 'react';
import { Box,
    HStack, 
    Stack, 
    Checkbox, 
    Input, 
    Text, 
    Textarea,
    Select,
    Divider,
    Fade,
    Button } from '@chakra-ui/react';

export default function MinimalistKit(){
    return (
        <Box
            boxShadow={'xl'}
            p={'6'}
            bg={'white'}
            w={'51vw'}
            h={'66vw'}
            alignSelf={'center'}>
        </Box>
    );
};